package new_school_progect.service;

import new_school_progect.model.Course;
import new_school_progect.model.Principal;
import new_school_progect.model.Student;

public interface StudentService {
    void displayExplledStudent(Student student, Course course, Principal principal);

   // void courseOffered (Student student, Course course);
}
